({
	onFormSubmit : function(component, event, helper) {		
		//This is the code that calls the public method of the search component
		//Uncomment on the integration step
		/*
		var teamMateId = event.getParam("teamMateId");
		var resultsComponent = component.find("teamMateSearchResults");
		console.log('Calling the method search');
		console.log(teamMateId);
		var result = resultsComponent.search(teamMateId);		
		*/
	}
})
